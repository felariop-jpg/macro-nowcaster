# Runtime dependencies for the DEPLOYED dashboard (Streamlit Community Cloud).
# The deployed app renders a precomputed snapshot (app/snapshot.json), so it does
# not need the modeling stack at view time. The full dependency set for running
# the entire system (statsmodels, hmmlearn, duckdb, fredapi, fastapi, ...) lives
# in pyproject.toml; install it locally with:  pip install -e ".[dev,app,llm]"
streamlit>=1.32
plotly>=5.18
pandas>=2.0
requests>=2.31
